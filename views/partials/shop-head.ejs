<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= settings.hero_subtitle %>">
  <meta name="theme-color" content="<%= settings.theme_color %>">
  <title>
    <%= title ? `${title} • ${settings.site_name}` : settings.site_name %>
  </title>
  <link rel="icon" href="<%= settings.favicon_url %>">
  <link rel="stylesheet" href="/css/main.css">
  <style>
    :root {
      --color-accent: <%=settings.primary_color %>;
      --color-page: <%=settings.secondary_color %>;
      --color-text: <%=settings.theme_color==='#0b0c0f' ? '#0b0c0f': '#0b0c0f' %>;
      /* Just ensuring theme color logic if needed later */
    }
  </style>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&family=Inter:wght@400;500;600&display=swap"
    rel="stylesheet">
  <script src="/js/main.js" defer></script>
</head>