<!DOCTYPE html>
<html lang="tr">
<%- include('../partials/shop-head', { title: product.name }) %>

  <body>
    <%- include('../partials/shop-header') %>
      <main class="page page-product">
        <section class="product-hero">
          <div class="container product-hero-inner">
            <nav class="breadcrumbs">
              <a href="/">Mağaza</a>
              <span>/</span>
              <span>
                <%= product.name %>
              </span>
            </nav>
            <div class="product-hero-copy">
              <h1>
                <%= product.name %>
              </h1>
              <p>
                <%= product.description %>
              </p>
              <div class="hero-meta">
                <span class="price">
                  <%= product.price.toFixed(2) %> ₺
                </span>
                <span class="stock">Stok: <%= product.stock %> adet</span>
              </div>
            </div>
          </div>
        </section>

        <section class="container product-detail">
          <%- include('../partials/flash') %>
            <div class="product-gallery">
              <% if (product.imageUrl) { %>
                <img src="<%= product.imageUrl %>" alt="<%= product.name %>">
                <% } else { %>
                  <div class="image-placeholder">
                    <span>Görsel Yakında</span>
                  </div>
                  <% } %>
            </div>
            <aside class="product-info">
              <div class="product-summary">
                <header class="product-summary__header">
                  <h2>
                    <%= product.name %>
                  </h2>
                  <span class="product-summary__price">₺<%= product.price.toFixed(2) %></span>
                </header>
                <form action="/cart/add" method="POST" class="add-form">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <input type="hidden" name="productId" value="<%= product.id %>">
                  <div class="add-form__quantity">
                    <select name="quantity">
                      <option value="1">Adet</option>
                      <option value="1">1</option>
                      <option value="2">2</option>
                      <option value="3">3</option>
                      <option value="4">4</option>
                      <option value="5">5</option>
                    </select>
                  </div>
                  <div class="add-form__actions">
                    <button type="submit" class="btn btn-primary">Sepete Ekle</button>
                    <a href="/cart" class="btn btn-outline">Sepeti Gör</a>
                  </div>
                </form>
                <ul class="product-details">
                  <li>
                    <span class="product-details__icon">📦</span>
                    <div>
                      <span class="product-details__label">PAKET:</span>
                      <span class="product-details__value">
                        <%= product.grams %> g
                      </span>
                    </div>
                  </li>
                  <% if (product.contentsText) { %>
                    <li>
                      <span class="product-details__icon">🌿</span>
                      <div>
                        <span class="product-details__label">İÇERİK:</span>
                        <span class="product-details__value">
                          <%= product.contentsText %>
                        </span>
                      </div>
                    </li>
                    <% } %>
                      <li>
                        <span class="product-details__icon">🚚</span>
                        <div>
                          <span class="product-details__label">TESLİMAT:</span>
                          <span class="product-details__value">
                            <%= settings.product_detail_delivery_text || '24 saat içinde kargoda' %>
                          </span>
                        </div>
                      </li>
                </ul>
              </div>
            </aside>
        </section>
      </main>
      <%- include('../partials/shop-footer') %>
  </body>

</html>